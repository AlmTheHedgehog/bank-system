#include <stdio.h>
#include "structs.h"
#include "functions.h"

int main(){

    printf("%ld", int_read("Hey:", isAccNumber));
    //AccNumber - Pesel and (sum of 2 last id numbers) at the beginning
    return 0;
}